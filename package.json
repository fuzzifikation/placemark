{
  "name": "placemark",
  "private": true,
  "version": "0.1.0",
  "description": "Privacy-first, local-first photo organizer",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "dev": "pnpm -C packages/desktop dev"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
